# Notes

- cron utils
- remote inter-actor communication via a remote protocol
- promise or tasks utils
- Inbound event triggers (webhooks?)
- running code in different runtime eg. run rust or python or go code in actors perhaps via wasm
- add support for supervision trees, an actor can be promoted to a supervisor and a supervisor could monitor supervisors, etc
